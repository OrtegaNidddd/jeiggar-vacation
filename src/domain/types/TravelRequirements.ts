export type TravelRequirementsHeroContent = {
  badge: string;
  title: string;
  description: string;
};

export type TravelRequirementItem = {
  id: string;
  title: string;
  image: string;
  alt: string;
  description: string;
};

export type TravelRequirementsCtaContent = {
  title: string;
  description: string;
  buttonLabel: string;
  buttonHref?: string;
};

export type TravelRequirementsLegalContent = {
  text: string;
};